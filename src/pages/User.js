import React, { Fragment } from 'react';
import { Helmet } from 'react-helmet';

const User = () => (
  <Fragment>
    <Helmet>
      <title>User</title>
    </Helmet>
    <h1>User</h1>
  </Fragment>
);

export default User;
